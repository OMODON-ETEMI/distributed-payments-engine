// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: outbox.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createOutboxEvent = `-- name: CreateOutboxEvent :one
INSERT INTO outbox_events (
    aggregate_type,
    aggregate_id,
    event_type,
    status,
    idempotency_key_id,
    payload,
    headers,
    partition_key
)
VALUES (
    $1,
    $2::uuid,
    $3,
    'pending',
    $4::uuid,
    $5::jsonb,
    $6::jsonb,
    $7
)
RETURNING id, aggregate_type, aggregate_id, event_type, status, idempotency_key_id, payload, headers, partition_key, published_at, retry_count, next_retry_at, error_code, error_message, created_at, updated_at
`

type CreateOutboxEventParams struct {
	AggregateType    string      `json:"aggregate_type"`
	AggregateID      pgtype.UUID `json:"aggregate_id"`
	EventType        string      `json:"event_type"`
	IdempotencyKeyID pgtype.UUID `json:"idempotency_key_id"`
	Payload          []byte      `json:"payload"`
	Headers          []byte      `json:"headers"`
	PartitionKey     pgtype.Text `json:"partition_key"`
}

func (q *Queries) CreateOutboxEvent(ctx context.Context, arg CreateOutboxEventParams) (OutboxEvent, error) {
	row := q.db.QueryRow(ctx, createOutboxEvent,
		arg.AggregateType,
		arg.AggregateID,
		arg.EventType,
		arg.IdempotencyKeyID,
		arg.Payload,
		arg.Headers,
		arg.PartitionKey,
	)
	var i OutboxEvent
	err := row.Scan(
		&i.ID,
		&i.AggregateType,
		&i.AggregateID,
		&i.EventType,
		&i.Status,
		&i.IdempotencyKeyID,
		&i.Payload,
		&i.Headers,
		&i.PartitionKey,
		&i.PublishedAt,
		&i.RetryCount,
		&i.NextRetryAt,
		&i.ErrorCode,
		&i.ErrorMessage,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listPendingOutboxEvents = `-- name: ListPendingOutboxEvents :many
SELECT id, aggregate_type, aggregate_id, event_type, status, idempotency_key_id, payload, headers, partition_key, published_at, retry_count, next_retry_at, error_code, error_message, created_at, updated_at
FROM outbox_events
WHERE status IN ('pending', 'failed')
  AND (next_retry_at IS NULL OR next_retry_at <= now())
ORDER BY created_at ASC
LIMIT $1
FOR UPDATE SKIP LOCKED
`

func (q *Queries) ListPendingOutboxEvents(ctx context.Context, limit int32) ([]OutboxEvent, error) {
	rows, err := q.db.Query(ctx, listPendingOutboxEvents, limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []OutboxEvent
	for rows.Next() {
		var i OutboxEvent
		if err := rows.Scan(
			&i.ID,
			&i.AggregateType,
			&i.AggregateID,
			&i.EventType,
			&i.Status,
			&i.IdempotencyKeyID,
			&i.Payload,
			&i.Headers,
			&i.PartitionKey,
			&i.PublishedAt,
			&i.RetryCount,
			&i.NextRetryAt,
			&i.ErrorCode,
			&i.ErrorMessage,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const markOutboxEventDeadLetter = `-- name: MarkOutboxEventDeadLetter :one
UPDATE outbox_events
SET status = 'dead_letter'
WHERE id = $1::uuid
RETURNING id, aggregate_type, aggregate_id, event_type, status, idempotency_key_id, payload, headers, partition_key, published_at, retry_count, next_retry_at, error_code, error_message, created_at, updated_at
`

func (q *Queries) MarkOutboxEventDeadLetter(ctx context.Context, id pgtype.UUID) (OutboxEvent, error) {
	row := q.db.QueryRow(ctx, markOutboxEventDeadLetter, id)
	var i OutboxEvent
	err := row.Scan(
		&i.ID,
		&i.AggregateType,
		&i.AggregateID,
		&i.EventType,
		&i.Status,
		&i.IdempotencyKeyID,
		&i.Payload,
		&i.Headers,
		&i.PartitionKey,
		&i.PublishedAt,
		&i.RetryCount,
		&i.NextRetryAt,
		&i.ErrorCode,
		&i.ErrorMessage,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const markOutboxEventFailed = `-- name: MarkOutboxEventFailed :one
UPDATE outbox_events
SET status        = 'failed',
    retry_count   = retry_count + 1,
    next_retry_at = now() + (power(2, retry_count) * interval '1 second'),
    error_code    = $1,
    error_message = $2
WHERE id = $3::uuid
RETURNING id, aggregate_type, aggregate_id, event_type, status, idempotency_key_id, payload, headers, partition_key, published_at, retry_count, next_retry_at, error_code, error_message, created_at, updated_at
`

type MarkOutboxEventFailedParams struct {
	ErrorCode    pgtype.Text `json:"error_code"`
	ErrorMessage pgtype.Text `json:"error_message"`
	ID           pgtype.UUID `json:"id"`
}

func (q *Queries) MarkOutboxEventFailed(ctx context.Context, arg MarkOutboxEventFailedParams) (OutboxEvent, error) {
	row := q.db.QueryRow(ctx, markOutboxEventFailed, arg.ErrorCode, arg.ErrorMessage, arg.ID)
	var i OutboxEvent
	err := row.Scan(
		&i.ID,
		&i.AggregateType,
		&i.AggregateID,
		&i.EventType,
		&i.Status,
		&i.IdempotencyKeyID,
		&i.Payload,
		&i.Headers,
		&i.PartitionKey,
		&i.PublishedAt,
		&i.RetryCount,
		&i.NextRetryAt,
		&i.ErrorCode,
		&i.ErrorMessage,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const markOutboxEventPublished = `-- name: MarkOutboxEventPublished :one
UPDATE outbox_events
SET status      = 'published',
    published_at = now()
WHERE id = $1::uuid
RETURNING id, aggregate_type, aggregate_id, event_type, status, idempotency_key_id, payload, headers, partition_key, published_at, retry_count, next_retry_at, error_code, error_message, created_at, updated_at
`

func (q *Queries) MarkOutboxEventPublished(ctx context.Context, id pgtype.UUID) (OutboxEvent, error) {
	row := q.db.QueryRow(ctx, markOutboxEventPublished, id)
	var i OutboxEvent
	err := row.Scan(
		&i.ID,
		&i.AggregateType,
		&i.AggregateID,
		&i.EventType,
		&i.Status,
		&i.IdempotencyKeyID,
		&i.Payload,
		&i.Headers,
		&i.PartitionKey,
		&i.PublishedAt,
		&i.RetryCount,
		&i.NextRetryAt,
		&i.ErrorCode,
		&i.ErrorMessage,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
